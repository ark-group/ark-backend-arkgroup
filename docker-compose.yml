version: "1.0"

services:
  sit-cache:
    image: ark-group/ark-backend-employee
    ports:
      - "8080:8080"
      - "6300:6300"
    links:
      - rabbit:rabbit
      - redis:redis
    environment:
      - HTTPS_PROXY=http://localhost:8080


  redis:
    image: ark-group/redis
    ports:
      - "6379:6379"

  rabbit:
    image: ark-group/rabbitmq
    ports:
      - "15672:15672"
      - "5672:5672"
